# MEMORY.md — Happy Photo Organizer

> ## ⛔ #1 HARD RULE — AGENT CAP (read BEFORE spawning ANY agent — the most-violated rule)
> Default **≤ 3 agents/round** · max **5** with a clear reason · **> 5 → STOP, ask Nick first**. Count worst-case before spawning; per-finding/file/page fan-out is forbidden unless you can name its cap.
> **"Unlimited tokens" ≠ unlimited agents** → Tester = exactly **3** (no sub-fan-out) · Lucifer = `lucifer.js` only (≤5/round). ❌ NEVER self-start Tester/Lucifer/any fleet — Nick must type the trigger.
> Cause: 24-agent burn 2026-06-10, repeated 2026-06-13 · = command_pattern #16.
>
> **🛑 FAIL = STOP, not retry (other half of #16):** a workflow/agent that returns fail / error / empty / unexpected → **report in plain words + wait for Nick**. ❌ never re-launch Tester/Lucifer yourself · ❌ never retry or tweak-args-and-refire · ❌ never loop silently · diagnose **0-agent** first. **1 Nick-trigger = 1 launch**; re-run only on Nick's word. Cause: 2026-06-13 args bug → 60-agent burn (6 Lucifer × 10) from blind retries.


> สแนปช็อต onboarding หลักของโปรเจคนี้ (ไฟล์เดียว) — Nick สั่ง "อ่านเมมโมรี่" = อ่านไฟล์นี้
> สร้างจากการ onboard ตาม MASTER Section 5 เมื่อ **2026-06-04** | Version ปัจจุบัน: **v1.042** (small-screen layout fix 2026-08-05)
> ✅ **Re-verified 2026-06-04** (session ใหม่ — วัดกับโค้ดจริง ไม่ใช่จำ): 32 files · 7,198 LOC · core 15 · ui 7 · main.py 1,240 · catalog 146 (121 bundled + 25 user) · 14 formats · `tests/test_core.py` 27/27 PASS — **ทุกตัวเลขตรงกับเมมโมรี่** (แก้จุดเดียว: main.py ~1,245 → 1,240)

---

## หมวด A — Path ของระบบหลัก

| ไฟล์ | Path |
|------|------|
| MASTER.md | `C:\Users\NickSuksanTr\Documents\Claude\Projects\Nick\MASTER.md` |
| SHARED.md | `C:\Users\NickSuksanTr\Documents\Claude\Projects\Nick\SHARED.md` |
| command_pattern.md | `C:\Users\NickSuksanTr\Documents\Claude\Projects\Nick\command_pattern.md` |
| Note Master.txt | `C:\Users\NickSuksanTr\Documents\Claude\Projects\Nick\Note Master.txt` |
| **โปรเจคนี้** | `C:\Users\NickSuksanTr\Documents\Projects\Happy-Photo-Organizer\` |

- วันที่อ่าน MASTER ครั้งล่าสุด: **2026-06-04** (context fresh)
- ⚠️ กฎ MASTER: หลังอ่าน+บันทึกเสร็จ → **ปิดไฟล์กลางทั้ง 4 ทุกครั้ง** (คอส + Coddy ใช้ไฟล์ชุดเดียวกัน)

---

## หมวด B — Gemini & AI Settings

| Setting | ค่า |
|---------|-----|
| Default Model | `gemini-3.5-flash-lite` (Nick ย้ายจาก 3.1 เมื่อ 2026-09-02 · ลิมิตฟรีเท่าเดิม · ห้ามเปลี่ยนเป็น Vertex / ห้าม hard-code free-only) |
| API Key Type | Google **AI Studio** เท่านั้น |
| Rate Limit | **RPM 15 / TPM 250,000 / RPD 500** (free tier) — เตือน Nick ก่อน batch ใหญ่ |
| Key location | `~/.happy-photo-organizer/auth.json` (atomic JSON, ไม่ hard-code ใน code) |

- ก่อนยิง Gemini เยอะ ต้องถาม Nick: (1) call กี่ครั้ง (2) free/paid key (3) ถ้าเกิน 500 RPD ต้อง throttle/split
- Settings ในแอปต้องให้เลือก pro/paid model ได้เสมอ
- รายละเอียด: ดู `GEMINI_LIMITS.md` ในโปรเจค

---

## หมวด C — Tools & Environment (จาก SHARED, ที่ใช้ในโปรเจคนี้)

| Tool | เวอร์ชัน |
|------|---------|
| Python | 3.13.13 (`...\Python313\python.exe`) |
| google-genai (Gemini SDK) | 1.75.0 |
| CustomTkinter | 5.2.2 (+ tkinterdnd2) |
| Pillow + pillow-heif | (รองรับ HEIC/iOS, 14 formats) |
| PyInstaller | 6.20.0 (folder mode) |
| Inno Setup | 6 (`ISCC.exe`) |
| Git | 2.54.0 |

---

## หมวด D — command_pattern 12 ข้อ (Quick Reference)

1. **Project Boundary** — ทำงานแค่ในโฟลเดอร์โปรเจคนี้ · โปรเจคอื่นอ่านได้/เอา idea ได้ แต่ห้ามแก้
2. **Gemini** — AI Studio key เท่านั้น · default `gemini-3.1-flash-lite` · RPM15/RPD500 · เตือนก่อน batch ใหญ่
3. **Branding** — Icon (camera) = Identity (taskbar/installer) · Mascot (robot) = Helper (welcome/dropzone) · ห้ามสลับ
4. **อัปเดต MEMORY** เมื่อพบ bug / code เสี่ยงสูง — ระบุ file/function/line + วิธีแก้
5. **Tester** trigger → spawn 3 agent ขนาน (functional gaps / code correctness / holistic) → แก้ทันทีไม่รอ approve → test → build → อัปเดต memory → รายงาน + สรุป token
6. **จัดโฟลเดอร์ให้เป็นระเบียบ** — มี `_trash\` (แยกหมวด, ห้ามลบตรงๆ ให้ย้าย)
7. **Changelog/log/bug** — แยกโฟลเดอร์ · max 20 entries/ไฟล์ · max 10 ไฟล์/ระบบ · เกินโยน `_trash`
8. **อ่าน memory (log/bug/changelog)** สูงสุด 5 ไฟล์ล่าสุด (=100 entries) ยกเว้น Nick สั่งอ่านหมด
9. **Log** — บันทึกการคุย+คำสั่งแยกตาม version ใน `log\log_vX.md`
10. **Bug Log** — บันทึก bug+fix แยกตาม version ใน `bug\bug_vX.md`
11. **V-Log** — timeline ทุกเวอร์ชันใน `V-Log.md`
12. **Lucifer** (routing) — พิมพ์ "Lucifer: <งาน>" → **ต้องรัน Workflow tool ด้วย scriptPath `C:\Users\NickSuksanTr\Documents\Claude\Projects\Nick\lucifer.js` เท่านั้น** (deterministic บังคับด้วยโค้ด) · ❌ ห้ามจำลอง 3 agent เอง · ❌ ห้ามเขียน+ให้คะแนนเอง · ❌ ห้าม build/อัพ ก่อนผ่านด่าน · Coddy เตรียม git worktree (args.workdir) + build/อัพ หลัง passed เท่านั้น (คอม: build + GitHub Release)

> หมายเหตุ: ข้อ 7–8 (log/bug/changelog หมุนตาม version) = คนละตัวกับ MEMORY.md (สแนปช็อตไฟล์เดียวนี้)

### กฎเสริมจาก Note Master.txt (Nick เขียนเอง)
- บันทึกทุกอย่างใน**โฟลเดอร์โปรเจคนี้เท่านั้น** · ทุกครั้งที่อัปเดต → **รันเทส + บันทึกลง memory**
- มีโฟลเดอร์ `_trash\` (ไฟล์ไม่ใช้แล้ว) + โฟลเดอร์ **ผู้ใช้** (ไฟล์ส่งออกให้ user ที่ไม่เกี่ยวกับโค้ดแอป เช่น screenshot หน้าตาแอป) — อย่าวางกระจัดกระจาย
- ก่อนเริ่มงาน: อ่าน md ในโปรเจคก่อน + ดูวิธีทำงานครั้งก่อนๆ
- จบงานทุกครั้ง: **สรุปงาน + อัปเดตให้ Nick รู้**
- ไม่จำกัด token/agent — ออกแบบให้ agent ทำงานขนานได้
- (เฉพาะแอปมือถือ: ตั้งชื่อ APK/AAB ชัดเจน + version, รันเทสใน emulator ก่อน build ขึ้น Play — *โปรเจคนี้เป็น desktop ไม่ใช้*)

### 🚀 Release = ทำเลย ห้ามถาม (Nick ย้ำ 2026-09-24)
- **"ปล่อยเลยไม่ต้องถามอีก"** — build + tag + GitHub Release ทำได้เองทุกครั้ง ไม่ต้องขออนุญาตทีละรอบ
  (ยืนยันซ้ำของเดิมจาก 2026-06-04 หลังจากผมถามซ้ำ 2 ครั้งในสัปดาห์เดียว)
- วิธีปล่อยที่ถูก (จากบทเรียน v1.050): **draft → อัปโหลด asset (background, 91 MB ใช้ ~15 นาที เกิน timeout เชลล์) → publish**
- สิ่งที่ยัง**ต้อง**ถาม: กฎ/พฤติกรรมที่เปลี่ยนผลลัพธ์งานของนิก (เช่น กฎวันที่) — คนละเรื่องกับการปล่อยรุ่น

### 🔀 Git workflow (Nick สั่ง 2026-06-04 — override ของเดิม)
- **เลิก PR/feature-branch — ทำงานบน `main` ตรงๆ** (เดิม `tester/* → PR → merge`; ปิดแล้ว — branch `tester/v1.041-audit-fixes` + PR #1 ลบ/ปิดทั้ง local+remote)
- **commit + push + build + ปล่อย GitHub Release ได้เลย ไม่ต้องถาม** — Nick ให้ standing authorization ("อัพขึ้นเลยตามปกติ ไม่ต้องถาม") → ทำ flow ปกติ (ดู RELEASE.md) แล้วรายงานท้าย

---

## หมวด E — สิ่งที่ต้องอัปเดต SHARED

- **ไม่มี** — SHARED.md (last updated 2026-06-03) ยังตรงกับความจริง: Python 3.13.13, google-genai 1.75.0, CustomTkinter 5.2.2, PyInstaller 6.20.0 ตรงกับที่โปรเจคใช้

---

## 📸 Project Snapshot (สำหรับ session ใหม่เข้าใจเร็ว)

**คืออะไร:** AI photo organizer สำหรับงานซ่อมบำรุงเรือ — drop รูป → auto date → resize 10-25KB → group by day → AI tag ชื่องาน (Gemini Vision) → review → rename folder `DD-MM-YY <Job>`

**Tech:** customtkinter 5.2 + tkinterdnd2 · google-genai (Gemini 3.1 Flash Lite) · Pillow + pillow-heif · PyInstaller + custom installer

**ไฟล์ AI สำคัญ (สำหรับงานเทส AI):**
- `core/analyzer.py` — Gemini Vision calls, `_is_transient_error` (regex 5xx), 5xx retry exponential backoff, Thai→EN translate, fuzzy catalog match (cutoff 0.85)
- `core/processor.py` — Phase 2 orchestration (parallel 4 workers ThreadPoolExecutor), date detection/allocation
- `core/rate_limiter.py` — tier presets + custom RPM/RPD + cancel-aware throttle + quota tracking (PT 00:00 reset)
- `core/catalog.py` — `data/job_catalog.json` (146 jobs), atomic save, RLock
- `core/auth.py` — API key (atomic auth.json + quarantine)
- `scripts/smoke_test.py` — มี smoke test (tests ใช้ stub `google.genai` — **AI จริงเทสได้แค่บนเครื่อง Nick + key จริง**)

**สถานะ:** v1.056 (2026-09-26 · **ปล่อย release แล้ว** · tests 130/130)
- 🧹 **v1.056: เคลียร์งานที่ลงเสร็จแล้วออกจากลิสต์ได้แล้ว** (นิกสั่ง "แก้ทั้ง 3 ตัวพร้อมกันเลย") — **ไม่ใช่ปุ่มเสีย แต่ไม่มีปุ่ม**: โค้ดที่ล้าง Step 3 อยู่ใน `_start_phase12` ที่เดียว = ล้างได้ทางเดียวคือสั่งรัน AI ใหม่ · ปุ่ม "Clear" ล้างแค่ Step 1
  - ปุ่มใหม่ **"Start a new batch"** โชว์ตลอด (ไม่ซ่อน — ปุ่มที่โผล่เฉพาะตอนจำเป็น คือปุ่มที่ไม่มีใครรู้ว่ามี) · ล้าง**เฉพาะลิสต์งาน** ไม่แตะรายการต้นทาง · `_reset_batch()` ตัวเดียวใช้ทั้งปุ่มและ `_start_phase12`
  - `JobAssignment.committed` → commit ซ้ำไม่ error อีก แต่แถวที่ถูกข้ามเพราะไม่มีชื่อ **ยัง commit ต่อได้** (อย่าปิดปุ่มทื่อๆ) · commit ล้าง**เฉพาะ source ที่ใช้ไปจริง** และต่อเมื่อลงครบทุกแถว
  - 🕳️ **Phase 1 เคยเดินกลับเข้าโฟลเดอร์ `__pending_` ที่ถูกทิ้งไว้** (`exist_ok=True`) → รูปของงานเก่าถูกลงในชื่องานใหม่ · สัญญาณเดียวคือจำนวนรูปเยอะผิดปกติ · แก้แล้ว (ของเก่ามีมานาน แต่ปุ่มใหม่ทำให้ "ทิ้งงาน" กลายเป็นคลิกเดียว)
- 🚨 **บทเรียนใหญ่สุดของ v1.056 อยู่ในเทส ไม่ใช่ในโค้ด (รีวิว 3 รอบ ทุกรอบบอกอย่าปล่อย):**
  - **เทสที่เรียก `messagebox` จริง = ค้างรอคนกดปุ่ม** · รันเดี่ยว timeout / รันทั้งชุดผ่าน → **เทสที่ผลขึ้นกับว่าอะไรรันมาก่อน ไม่ใช่ประตู** · ต้อง stub dialog ทุกตัว
  - **เทสที่ผ่านโดยไม่ได้ทดสอบอะไร (vacuous)** — วางโฟลเดอร์ปลอมคนละวันกับที่โค้ดจะสร้าง = ชนกันไม่ได้ตั้งแต่แรก · **ต้องพิสูจน์ว่าเทสแดงบนโค้ดเก่าเสมอ**
  - **ไม่มีเทสไหนยืนยันว่าปุ่มที่นิกขอ "กดได้จริง"** → ถ้า hard-code ให้เทาไว้ บั๊กเดิมกลับมาโดยเขียวหมด
  - **`except Exception: pass` รอบ `mark_filed` กลืน TypeError** ตอนผมเพิ่มพารามิเตอร์ → ฟีเจอร์ตายเงียบ เทสเขียว · เทสเชิงพฤติกรรมจับได้ เทสที่เช็คแค่ชื่อฟังก์ชัน (AST) จับไม่ได้
- 🔗 **v1.055: 3 โปรแกรมถูกหมด แต่ "รอยต่อ" ไม่มีอยู่จริง** — `emr.json` เรียกรูปด้วยชื่อของมือถือ (`0001.jpg`) แต่ในคลังรูปถูกเปลี่ยนชื่อเป็น `26-09-26 <งาน>_001.jpg` ตั้งแต่ v1.045 → EMR สแกนแล้วได้ **0 แมตช์** · และกฎของ EMR คือชื่อที่หาไม่เจอ **ข้ามแบบเงียบๆ** = รายงานออกมาโดยไม่มีรูปสักใบ แต่ขึ้นว่า "ใส่ร่างแล้ว" · **หาเจอได้เพราะฝั่งที่อ่านโฟลเดอร์ลองสร้างโฟลเดอร์เอง** ไม่ใช่การรีวิวโค้ด
- 🗺️ **แผนที่มีอยู่แล้วตอนลงงาน แล้วถูกทิ้ง** — `rename_photos_for_folder()` คืน `{ชื่อมือถือ: ชื่อในคลัง}` อยู่แล้ว · งานนี้คือ "เก็บไว้" ไม่ใช่ "คำนวณใหม่" → เขียนลง `filed.renamed` ใน manifest ของแต่ละงาน
- ⚠️ **ต้องแยกรายงาน ห้ามรวมเป็นแผนที่เดียวของโฟลเดอร์** — ทุกมือถือนับรูปของตัวเองเริ่มที่ 0001 โฟลเดอร์ที่ merge กันจึงมี `0001.jpg` สองใบ · แผนที่แบน = ช่องเดียว = รายงานงานหนึ่งไปชี้รูปของอีกงาน (JS ทักก่อนผมจะเขียน — **บั๊กตระกูลเดียวกับ `emr-<job_id>.json` แค่ลึกลงไปอีกชั้น**)
- 🪤 **จับคู่ด้วยชื่อไฟล์ = ผิดในเคสหนึ่ง** — ถ้างานแรก**ไม่มี**ร่าง ร่างของงานที่สองจะได้ชื่อ `emr.json` เฉยๆ (ไม่ชนใคร) แต่ manifest ของมันคือ `job-<id>.json` → จับคู่ตามชื่อจะยกร่างให้ผิดงาน · **ตัวเชื่อมที่ถูกต้องคือ `filed.extras`** (manifest ไหนบอกชื่อร่างไว้ manifest นั้นคือเจ้าของ) · พิสูจน์ด้วยการรัน
- 🚫 **ไม่เอาขึ้นสายลวด** — JS บอกตรงๆ ว่าจะอ่านแต่ไม่ใช้ (มือถือไม่ควรเริ่มสนใจว่าคลังตั้งชื่ออะไร) และ EMR อ่านจากโฟลเดอร์ ไม่ได้อ่านจาก socket → ใส่ใน manifest ที่เดียว
- 🧩 **"merged" ไม่ใช่เงื่อนไขเดียว — การชนชื่อเกิดเป็น"รายไฟล์" และสองไฟล์ของงานเดียวกันอาจไม่ตรงกัน** (JS สรุปหลังโดนเคสนี้ · โค้ดผมเช็ค `target.exists()` ทีละไฟล์อยู่แล้ว ซึ่งเป็น**เหตุ**ที่ทำให้เกิดความไม่สมมาตร) → ห้ามอนุมานสถานะของไฟล์ B จากชื่อของไฟล์ A ในงานเดียวกัน · **ให้ผูกด้วยข้อมูลที่เขียนไว้ (`filed.extras`) ไม่ใช่ด้วยรูปแบบชื่อไฟล์** · ตระกูลเดียวกับ [[a-comment-is-not-evidence]] — รูปแบบชื่อไฟล์ก็เป็นแค่"คำบรรยาย"ของความจริง ไม่ใช่ความจริง
- 🐛 **เปิดค้างไว้ (นิกสั่ง "จดไว้ ค่อยแก้ครั้งหน้า ไม่รีบ" 26-09-26) → `bug/bug_v1.055.md`** — **ลงงานเสร็จแล้วเคลียร์ลิสต์ไม่ได้ ต้องปิดเปิดแอป** · สาเหตุ: **ไม่มีปุ่มเคลียร์อยู่จริง** — โค้ดที่ล้าง Step 3 มีที่เดียวคือใน `_start_phase12` (`main.py:1473-1481`) กดถึงได้ทางเดียวคือสั่งรัน AI รอบใหม่ · ปุ่มที่ชื่อ "Clear" (`main.py:372`) ล้างแค่ `source_paths` ไม่แตะ Step 3 เลย
  - ⚠️ **เจอเพิ่ม 2 ตัวที่นิกยังไม่เจอ:** (1) ปุ่ม **Commit Rename ยังกดได้อีกรอบ**บนงานที่ลงไปแล้ว → error ทุกแถว + เด้ง "All Done" ซ้ำ (temp_folder ไม่มีแล้ว) · (2) **commit ไม่ล้าง `source_paths`** → ถ้าลากงานใหม่เข้าโดยไม่กด Clear **รูปของงานเก่าถูกประมวลผลซ้ำทั้งชุด** (เปลือง quota + ได้โฟลเดอร์ซ้ำ)
  - 🔗 **(2) ต้องแก้พร้อมกับตัวหลัก** — ตอนนี้นิกรอดเพราะการปิดเปิดแอปล้าง `source_paths` ให้เอง · ถ้าใส่ปุ่มเคลียร์แล้วไม่แก้ข้อนี้ = เอาอุบัติเหตุที่ช่วยไว้ออก แล้วเปิดบั๊กแทน
  - 🪝 **`tools/pre-commit` ติดตั้งแล้ว 26-09-26** (นิกสั่ง) — ทดสอบกับ commit จริงแล้วว่า git ปฏิเสธจริง · 0.3 วิ ตรวจเฉพาะไบต์ที่มองไม่เห็น · **อยู่ใน `.git/hooks` ซึ่ง git ไม่ track → มีผลแค่เครื่องนี้**
- ♻️ **บทเรียนที่เขียนไว้แล้วแต่ไม่ช่วยอะไร (26-09-26)** — `a-pipeline-exit-code-belongs-to-the-last-stage` อยู่ใน `memory/SHARED_LESSONS.md:112` ตั้งแต่ **05-09** เขียนโดยผมเอง (เคส `adb push` 7 ครั้งที่ขึ้น ok ทั้งที่พัง) · **วันนี้ผมทำซ้ำทั้งวัน** (`python tests/test_core.py | tail -4 && git commit`) และ EMR ก็ทำซ้ำในวันเดียวกัน
  - **ทำไมรู้แล้วยังพลาด:** `| tail -N` เป็น**รีเฟล็กซ์ประหยัด context** ไม่ได้คิดก่อนพิมพ์ · และของเดิมสอนวิธี **ตรวจจับ**ความเสียหาย (`PIPESTATUS`, เก็บ `rc`) ไม่ได้สอนว่า**เมื่อไหร่ห้ามสร้างมันขึ้นมา** → **ข้อเท็จจริงเกี่ยวกับกับดัก ไม่เปลี่ยนนิสัย · กฎต้องเขียนเป็นนิสัย**
  - ✅ **กฎที่ใช้จริงตอนนี้: ห้ามใส่ท่อให้คำสั่งที่ความสำเร็จของมันเป็นตัวตัดสินขั้นต่อไป** (จะย่อ output ก็ทำหลังเก็บ rc) · และ 2 คำสั่งบนบรรทัดเดียวคั่นด้วย `;` **ไม่ได้กั้นอะไรเลย** มีแต่ `&&` ที่กั้น
  - 🔍 เช็คแล้วว่า**ไม่มีสคริปต์ที่ commit ไว้ตัวไหนมีบั๊กนี้** — มันอยู่ในนิสัยการพิมพ์ของผมล้วนๆ (`tools/pre-commit` = ยาม 0.3 วิ เฉพาะไบต์ที่มองไม่เห็น · **ยังไม่ติดตั้ง รอนิกตัดสินใจ**)
  - ⚠️ **ห้ามแก้ `memory/SHARED_LESSONS.md` ในโปรเจค** — เป็นสำเนา sync จะทับ · ตัวจริงอยู่นอกโปรเจค (EMR แก้ตัวจริงให้แล้ว รอ sync มาเอง)
- 📊 **สกอร์สายนี้ 26-09-26: บั๊ก 4 ตัว — และทั้ง 4 ตัวถูกเจอโดยฝั่งที่"ไม่ได้เขียนโค้ดนั้น"** (คำถาม JS เจอของผม · คำถามผมเจอของ JS 2 ตัว · โฟลเดอร์ของ EMR เจออีกตัว) · **ทุกตัวพังแบบเงียบและดูเหมือนทำงานสำเร็จ · ทุกตัวอยู่ใน"รอยต่อ"ที่เทสของฝั่งเดียวเอื้อมไม่ถึง** — ไม่ใช่เพราะใครสะเพร่า แต่เพราะแต่ละฝั่งเทสครึ่งที่ตัวเองเห็นแล้วถูกต้องจริงๆ
- 📋 **v1.054: ใบงานใบเดียวส่งต่อกันทั้งสาย (นิกสั่ง 2026-09-26)** — JS → HPO → EMR → Director แต่ละคนกรอกสิ่งที่**ตรวจจริง**แล้วส่งต่อทั้งฉบับ (`docs/CHAIN_CHECKLIST.md`) · **การนั่งกรอกให้ซื่อสัตย์คือสิ่งที่ทำให้เจอบั๊ก 2 ตัว ฝั่งละตัว** — ไม่ใช่การรีวิวโค้ด
- 🧾 **§4 (`GET /jobshot/v1/job/<id>`) เงียบเรื่องไฟล์ร่าง** — เส้นทางนี้มีไว้ใช้ตอน reply หาย ซึ่งคือตอนที่มือถือมืดที่สุดพอดี · ตอบแค่ว่ารูปลงแล้ว ไม่บอกว่าไฟล์ที่นิกพิมพ์เองรอดไหม → ใส่ `extras` แล้ว
- 🗳️ **ไม่แก้สายลวดเองทั้งที่รู้ว่าถูก — ยื่นขอในใบงานก่อน** (บทเรียน v1.049) · JS โหวตผ่าน **และเจอบั๊กฝั่งตัวเองจากคำถามนี้**: `markFiled` ของเขา*แทนที่* extras ด้วยของใบล่าสุด → กดส่งซ้ำ แล้ว §4 ตอบลิสต์ว่าง = **ลบคำยืนยันที่พีซีเคยให้ไปแล้ว** · การถามในใบงานได้ผลมากกว่าการแก้เงียบๆ
- ❓ **"ไม่มีคีย์" ≠ "ว่าง"** — entry เก่าจาก v1.053 ไม่มีคีย์ `extras` เลย ถ้าตอบ `[]` = บอกมือถือว่า ไฟล์ร่างที่มันถืออยู่ไม่เคยถูกยืนยัน → entry ที่ไม่มีคีย์ ให้ไปอ่าน manifest ในคลังแทน
- 🗣️ **ข้อความที่อีกฝั่งเอาไปโชว์ให้นิกอ่านตรงๆ ต้องบอกว่าให้ทำอะไรต่อ** — `"not a job"` ถูกพิมพ์เป็น *"The PC skipped this job: not a job."* = ฟังเหมือนจบเห่ ทั้งที่ความจริงคือ "ส่งใหม่สิ" → เปลี่ยนเป็น `"no job.json in it — send the job again"`
- 🧊 **เทส "ล็อกสัญญา" ที่ล็อกไม่ครบ = เงียบผิดแบบ** — §2/§3 ล็อกชุดคีย์ไว้ แต่ §4 เช็คแค่บางคีย์ เลยเพิ่มคีย์เข้าไปได้โดยไม่มีเทสแดงสักตัว · ล็อกครบแล้ว
- 📄 **v1.053: ไฟล์ร่างรายงาน (`emr.json`) เดินทางไปกับงานได้แล้ว** — JobShot ถามก่อนสร้างของบนสมมติฐาน **และคำตอบจริงแย่กว่าที่เขากลัว**: zip ไม่ถูกปฏิเสธ แต่ `_bad_entry()` ยอมแค่ `^job(-…)?\.json$` → ไฟล์ร่าง**ถูกทิ้งตอนแตกไฟล์** · ต่อให้แตกออกมาได้ก็ไม่ถูกก๊อป เพราะรายการไฟล์มาจาก `manifest["photos"]` · และ**ใบเสร็จไม่พูดถึงมันเลย** ทั้งที่ `filed` = สิ่งที่มือถือแปลว่า *ลบจากเครื่องได้แล้ว* → นิกอาจลบสำเนาเดียวของสิ่งที่ตัวเองพิมพ์
- 🚪 **เปิดประตูให้เพื่อน = จังหวะที่ประตูถูกเปิดให้ทุกคน** — สายลวดรับ **รูป + JSON เท่านั้น** ที่เหลือไม่ขยับเลย (traversal / absolute path / ชื่ออุปกรณ์ / ratio / cap) · มีเทสยืนยันว่า `notes.txt`, `run.exe`, `photo.jpg.exe` ยังถูกปฏิเสธ
- 📋 **ก๊อป ไม่ใช่ย้าย** — โฟลเดอร์ต้นทางเป็นของผู้ส่ง ฟังก์ชันนี้ต้องไม่ใช่เหตุที่มือถือเสียสำเนาสุดท้าย · 2 งาน merge โฟลเดอร์เดียวกัน → ตัวที่สองเป็น `emr-<job_id>.json` ไม่ทับของแรก (คนละงาน คนละเนื้อหา)
- 🧾 **ไฟล์ที่ reply ไม่เอ่ยถึง ห้ามนับว่าลงแล้ว** — ก๊อปไม่สำเร็จ = warning และ**ไม่ใส่ใน `extras`** · `extras` รายงาน*สิ่งที่อยู่บนดิสก์* ไม่ใช่*สิ่งที่ถูกส่งมา*
- 📅 **กฎวันของนิก (ส่งต่อมาทาง EMR แล้ว JobShot): "วันซ้ำได้ต่อเมื่อเดือนเต็ม"** — **เป็นแบบนั้นอยู่แล้วตั้งแต่ v1.026** (`_find_free_day_earliest`) · **พิสูจน์ด้วยการรัน ไม่ใช่การอ่าน** (1–29 เต็ม → ได้ 30 · เต็ม 30 → `None` = วนใช้วันตัวเอง) · ไม่ต้องแก้โค้ด แต่**ไม่เคยมีเทส** ซึ่งสำหรับกฎที่ถูกย้ำขนาดนี้คือช่องโหว่ → ใส่เทสล็อกแล้ว
- 📅 **v1.052: "วันที่ผิด" คือกฎ ไม่ใช่บั๊ก** — 3 งานส่งวันเดียวกันได้ `24-09-26`, `04-09-26`, `18-09-26` เพราะกฎ "เลขวันห้ามซ้ำทั้งคลัง" (นิกยืนยันเป็นครั้งที่ 3 ว่าเอาแบบนี้) · แก้ที่ **การอธิบาย ไม่ใช่กฎ**: reply ส่ง `work_date` ไปด้วย (มือถือจะบอกได้ว่า *ย้ายมาจาก 24-09-26*) + log ฝั่งพีซีบอกทั้งสองวันและเหตุผล · ฝั่งมือถือยังต้องให้ JobShot แสดงผลเอง
- 🔎 **อัปเดต 2026-09-26: `jobshot_filed.json` ยัง**ไม่มี** แต่สแกนแล้วพบว่า **ใต้ปลายทางที่จำไว้ไม่มีโฟลเดอร์ที่ลงงานเลยสักอัน** (งาน 3 ชิ้นของ 24-09 ถูกย้าย/ลบไปแล้ว) → แปลว่า **ยังไม่เคยมีการลงงานบนเครื่องนี้หลัง v1.052 ที่ทำให้มันร้อง** · สถานะที่ถูกต้องคือ **"ยังไม่เคยถูกทดสอบ" ไม่ใช่ "พังเงียบ"** — ผมเคยจดผิดเป็นอย่างหลัง
- 🔇 **`except Exception: pass` ทำให้ fast path ตายเงียบ** — receipt file ไม่เคยถูกเขียนเลยทั้งที่ลงงานสำเร็จ 3 งาน (§4 ยังตอบถูกเพราะ fallback ไปสแกน manifest) · หาสาเหตุจากนอกแอปไม่ได้ → **ทำให้มันร้องเอง** แทนการเดา
- 🎲 **เทส flaky ตัวที่ 2 แบบเดียวกัน** (`token[:-1] + "0"`) — เมื่อวานแก้ตัวแรกแล้วไม่ได้ไล่หาพี่น้อง · **บั๊กที่เป็นแพทเทิร์น ต้อง grep หาที่เหลือก่อนปิดงาน**
- 🖼️ **v1.051 บทเรียนใหญ่: ผมแก้ UI โดยไม่เคยเห็น UI** — หัวแอปสูง 92 px ตายตัว แต่ผมวางปุ่มซ้อน 4 ปุ่ม = ต้องใช้ 132 px → Phone โดนตัดครึ่ง · **ปุ่มอัพเดทหายทั้งปุ่ม** (ปุ่มที่ทำมาเพื่อให้เห็นระบบอัพเดท กลับเป็นปุ่มที่มองไม่เห็นเอง) · เพิ่มคนละวัน ไม่เห็นทั้งคู่ · **กฎใหม่: งานที่แก้หน้าต่าง ยังไม่จบจนกว่านิกจะส่ง screenshot มายืนยัน** (grep หา entry point จับเรื่อง "อยู่บนจอจริงไหม" ไม่ได้)
- 🔌 **"เชื่อมต่อไม่ได้" = ตรงกันข้าม** — log มี `GET /jobshot/v1/hello` ตอบ 200 สามครั้ง มือถือจับคู่ได้แล้ว · ที่ค้างคือ **พีซียังไม่ได้เลือกโฟลเดอร์ปลายทาง** (ข้อความที่ผมใส่เองใน v1.049 ถูกต้องแต่ปุ่มแก้อยู่หลังหน้าต่างที่คนกำลังมอง) → หน้าจับคู่แยก **paired ≠ ready** + มีปุ่มเลือกโฟลเดอร์ในตัว
- ⏱️ **อัปโหลด asset 91 MB ใช้เวลา ~15 นาที** — เกิน timeout ของเชลล์ (สูงสุด 10 นาที) ต้องรัน background เสมอ · v1.051 ใช้ draft-first สำเร็จแล้ว (ไม่มี release เปล่าโผล่บนเน็ตอีก) — รวม v1.048/49 ที่ค้างไว้เข้าไปด้วย · นิกสั่ง "รีลิสเลย")
- ⚠️ **บทเรียนตอนปล่อย v1.050**: อัปโหลด asset 90 MB ผ่าน API **เกิน 10 นาที** จนโดนฆ่ากลางคัน → เหลือ **release สาธารณะที่ไม่มีไฟล์แนบ** อยู่พักหนึ่ง · ไม่มีใครเดือดร้อนเพราะ `check_for_update()` เจอ release ที่ไม่มี asset แล้ว `return None` (= ไม่มีอัปเดต) **แต่นั่นคือรอดโดยบังเอิญ ไม่ใช่โดยออกแบบ** · `RELEASE.md` เพิ่มกฎแล้ว: **สร้างเป็น draft → อัปโหลด asset → ค่อย publish** และรันอัปโหลดแบบ background เสมอ
- 🔘 **v1.050: ปุ่มอัพเดทในหัวแอป** (นิกสั่ง: "ไม่รู้ไม่เห็นอะไรเลย กดก็ไม่ได้") — เดิมมีแค่ในเมนูคลิกขวาที่ tray และ **ผมเคยบอกนิกผิดว่า "กด Check for updates ในแอป" ทั้งที่ไม่มีปุ่มนั้นอยู่จริง** · ปุ่มบอกสถานะจริงทุกแบบ (`Checking…` · `Up to date (1.050)` · `Updates: offline` · `Downloading 37%` · `Install v1.051`) · เช็คแล้วไม่เจอ = **พูดออกมา** ไม่ใช่เงียบ · จะติดตั้ง = **ถามก่อน** · **ห้ามติดตั้งกลางแบตช์** (รีสตาร์ท = งานหาย)
- 🧠 ตรรกะอยู่ที่ `UpdateWorker.describe()` ใน core (เทสได้โดยไม่ต้องเปิดหน้าต่าง · หน้าต่างแค่เลือกสี) — เหตุผลเดียวกับ `split_arrivals`
- 🎲 **เจอเทสที่ตกได้ 1 ใน 16 ครั้ง**: มันสร้าง "token ผิด" ด้วยการแทนตัวท้ายด้วย `"0"` ซึ่งกลายเป็น token จริงถ้าของจริงลงท้ายด้วย 0 อยู่แล้ว · **เทสที่ตกนานๆ ทีสอนให้คนรันซ้ำจนผ่าน** = อันตรายกว่าเทสที่ตกทุกครั้ง
- 📜 **v1.049: เอกสาร = สัญญา ไม่ใช่คำอธิบายสัญญา** — ผมหยิบ path จาก `JobShot/docs/LAN_PROTOCOL.md` มาใช้ แต่ **ประกาศรูปแบบ reply เองจากหน้าเดียวกันที่ไม่ได้อ่าน** → ฝั่งมือถืออ่าน `filed` เป็น bool (ตามเอกสาร) แต่ผมส่ง list → **อัปโหลดสำเร็จสมบูรณ์ถูกรายงานให้นิกว่าพีซีปฏิเสธงาน** · ของผมถูกตัดสินว่าดีกว่าและเอกสารถูกแก้ตาม แต่ **การเบี่ยงที่ไม่มีใครจดไว้ = บั๊ก ถึงโค้ดจะดีกว่าก็ตาม** · อ่านทั้งฉบับแล้วเจอเพิ่มอีก 2 จุด (`app` เป็นชื่อสวยๆ ทั้งที่ §2 กำหนดเป็น id ที่มือถืออาจเทียบ · `ready:false` ไม่บอกเหตุผล) · ตอนนี้ **มีเทส 4 ตัวล็อก "รูปร่าง" ของสายลวด** (key + type ของ reply/ping/receipt · `filed` เป็น **list** ใน §3 แต่เป็น **bool** ใน §4) — เทสแดง = ถามว่า"อีกฝั่งขยับหรือยัง" ไม่ใช่ไปแก้เทส
**v1.048 ที่ต้องจำ — รับงานจากมือถือผ่าน Wi-Fi:**
- 📡 **3 ไฟล์ใหม่**: `jobshot_receive.py` (ตรวจ zip ที่มาจากเน็ต = ส่วนอันตรายสุด) · `jobshot_server.py` (socket บาง 2 route) · `jobshot_index.py` (สมุดใบเสร็จ) · UI: ปุ่ม **Phone** + `ui/dialogs/pairing.py` (QR)
- 🛡️ **zip-slip = ความเสี่ยงอันดับ 1** — ชื่อไฟล์ใน zip จากเน็ตเวิร์ก **ไม่ใช่ path แต่เป็นข้อความที่คนส่งคุมได้** · whitelist + **resolve ซ้ำเทียบกรงกักกันหลัง join** (`ZipFile.extract` ก็ sanitize แต่ "library คงจัดการให้" ไม่ใช่เหตุผลด้านความปลอดภัย)
- 🧾 **`GET /jobshot/v1/job/<id>` = ใบเสร็จที่อยู่นานกว่า request** — reply หายไม่ใช่จุดจบ · **สมุดใบเสร็จเป็น cache · โฟลเดอร์จริงคือความจริง** (เปลี่ยนชื่อ→ตอบชื่อใหม่ · ลบ→ตอบ 404 ให้มือถือเก็บไว้ · สมุดหาย→สแกนเอา) · ทุกทางที่ลงงาน (LAN/ลากวาง/สคริปต์) เขียนใบเสร็จหมด
- 🐛 **ตอบ error โดยไม่อ่าน body → Windows รีเซ็ตคอนเนคชัน** → มือถืออ่านว่า "Wi-Fi หลุด" ทั้งที่จริงคือ "token ผิด" (เจอจากการ**รันเทส** ไม่ใช่การอ่านโค้ด)
- 🔌 **QR ต้องพก port ที่ bind ได้จริง** ไม่ใช่ค่า default (8765 ไม่ว่าง → ขอพอร์ตใหม่จาก OS)
- ⚠️ **`no-entry-point-is-not-finished` เกิดซ้ำกับฟีเจอร์ที่ให้กำเนิดบทเรียนนี้เอง** — receiver เสร็จ+ปลอดภัย+เทสผ่าน แต่ไม่มีอะไรใน UI เรียกมัน · Director จับได้ด้วย grep 10 วินาที · **ต้องเช็คแบบกลไก ไม่ใช่ใช้วิจารณญาณ**
- 🔥 **Windows Firewall เด้งครั้งแรกที่เปิดพอร์ต** — ถ้าไม่กด Allow อาการคือ "มือถือบอกส่งแล้วแต่ไม่มีอะไรมา" = แยกไม่ออกจากบั๊ก (เขียนเตือนไว้บนหน้าจับคู่แล้ว)
- ⏭️ **ยังไม่ทำ:** mDNS (QR คือทางที่ต้องเวิร์ค · หลักฐาน Tuya: UDP announce เจอ 1/3 เครื่อง แต่ TCP sweep เจอ 3/3)
**v1.047 ที่ต้องจำ — ทางเข้าของ JobShot (นิกใช้งานจริง 2026-09-23):**
- 🚪 **v1.046 เขียนเสร็จแต่ "เรียกใช้ไม่ได้"** — `import_job()` ไม่มีอยู่ในไฟล์ UI เลยสักไฟล์ · บทเรียน: โค้ดที่ไม่มีทางเข้า = ยังไม่เสร็จ ไม่ว่าเทสจะเขียวแค่ไหน
- 🎯 **3 ทางเข้า**: drop zone (ลากโฟลเดอร์งาน หรือโฟลเดอร์แม่ที่ก๊อปมาทั้งก้อน) · ปุ่ม **From phone** · `python scripts/import_job.py <folder> --dest <folder>` (ใช้ได้ตอนแอพเปิดไม่ขึ้น/เครื่องไม่ได้ติดตั้งแอพ)
- 🧭 **`jobshot.split_arrivals()`** ตัดสินว่าอะไรคืองานจากมือถือ: โฟลเดอร์ที่มี `job.json` = งาน · โฟลเดอร์ที่**ลูกของมัน**มี `job.json` = โฟลเดอร์แม่ · ที่เหลือ = รูปธรรมดา ไปทางการ์ดเหมือนเดิม (กฎนี้อยู่ใน core ไม่ใช่ main.py → เทสได้โดยไม่ต้องเปิดหน้าต่าง)
- ⏳ โฟลเดอร์ที่ยังไม่มี `job.json` = **ยังโอนไม่เสร็จ** → บอกว่า "ยังไม่พร้อม" ไม่ใช่ error · ห้ามแตะ ห้ามลบ
- 🧪 **รันเทส end-to-end แล้วเผลอเขียนทับข้อมูลจริง** — สคริปต์บันทึกชื่องานลง `data/job_catalog.json` **ตัวจริง** (174 → 176 ชื่อปลอม) เพราะการ record usage ถูกต้องสำหรับงานจริงแต่ผิดสำหรับการทดลอง · จับได้ตอนอ่าน `git diff` ก่อน commit · คืนค่าแล้ว + ตรวจ payload ในตัวติดตั้งว่าสะอาด + เพิ่ม `--catalog` ไว้ชี้ไปที่อื่นตอนทดลอง · **ตระกูลเดียวกับบั๊ก installer ของ v1.044** — ไฟล์ข้อมูลผู้ใช้ที่อยู่ในกองโค้ด
- 🐛 **version gate รับ `true` และ `1.0` ผ่าน** (`True == 1` และ `1.0 == 1` ใน Python) — EMR เจอในตัวเอง แล้วส่งต่อมา · ผมลอง**ยิงจริง**เลยเจอเพิ่มว่า `1.0` ก็ผ่าน · แก้เป็นบังคับ `int` แท้ · **บทเรียน: เทส gate ต้องกวาดรูปแบบผิดทั้งชุด ไม่ใช่เทสแค่ค่าที่ถูก**
**v1.046 ที่ต้องจำ — JobShot (แอพมือถือ) ส่งงานเข้า HPO:**
- 📥 **`core/jobshot.py`** = ทางเข้าใหม่ (ทางการ์ดเดิมไม่เปลี่ยนพฤติกรรม) · มือถือส่ง **ต้นฉบับ** + `job.json` (เขียนท้ายสุด = ตัวบอกว่าโอนจบ · ไม่มีไฟล์นี้ = ยังโอนไม่เสร็จ ห้ามแตะ/ห้ามลบ) · `import_job()` = resize → กฎวันที่ → commit → เขียน manifest ลงโฟลเดอร์ปลายทาง · **ไม่ re-group ไม่เรียก AI ตั้งชื่อ** (คนหน้างานจัดกลุ่ม+ตั้งชื่อมาแล้ว · ถ้าปล่อยให้จัดกลุ่มตามวัน = 2 งานคนละงานวันเดียวกันถูกยุบเป็นโฟลเดอร์เดียวเงียบๆ)
- 🔗 **rename ของ v1.045 เกือบทำ manifest หาย** — `rename_photos_for_folder` เปลี่ยนชื่อ*ทุกไฟล์* → `job.json` จะกลายเป็น `<job>_003.json` · ตอนนี้ **ข้ามไฟล์ที่ไม่ใช่รูป** + **คืน map ชื่อเก่า→ใหม่** (เก็บที่ `JobAssignment.photo_renames` · merge ที่เปลี่ยนชื่อซ้ำก็อัปเดต map ให้) → `photos[]` ตามทันเสมอ (เหตุผลเดิมคือแท็ก Before/After · ตอนนี้แท็กถูกตัดแล้ว แต่ยังเก็บไว้เพราะ receive log ต้องใช้ + manifest ที่ชี้ไฟล์ที่ไม่มีอยู่ = กับดักของคนอ่านคนถัดไป) · **ส่วนที่ข้ามไฟล์ที่ไม่ใช่รูป ห้ามถอด** (ป้องกันตัว marker เอง)
- 📅 **นิกตัดสิน 2026-09-22: กฎคลัง (เลขวันห้ามซ้ำ + ยุบเข้าเดือนหลัก) ชนะ `work_date` จากมือถือ** → วันที่โฟลเดอร์ถูกเลื่อนได้ · เก็บความจริงไว้: `work_date` ไม่ถูกแก้ + เพิ่มบล็อก `filed` (folder/folder_date/date_shifted/merged) · **งานที่มีโฟลเดอร์อยู่แล้ว = merge เข้าไป** ไม่ใช่เลื่อนไปวันว่าง (ดูข้อถัดไป)
- 🧩 **1 งานจริง = 1 โฟลเดอร์ (นิกสั่ง 2026-09-22 — ไปไกลกว่ากฎวันที่)**: `find_filed_job()` อ่าน manifest ในโฟลเดอร์ที่จัดไปแล้ว แล้วจับคู่ด้วย **job_name + work_date** (❌ ไม่มี ship — `dest_root` แยกตามเรืออยู่แล้ว และชื่อเรือพิมพ์ไม่เหมือนกันจะทำให้งานเดียวแตกเป็น 2 โฟลเดอร์) → งานเดิมอยู่โฟลเดอร์ไหนก็เจอ แม้กฎวันที่จะเลื่อนวันโฟลเดอร์นั้นไปแล้ว · เหตุ: กฎเลขวันไม่ซ้ำกับ merge-ตอนชื่อชน **ขัดกันโดยโครงสร้าง** (A อยู่วัน 22 → B โดนเลื่อนไปวันว่าง → ชื่อไม่เคยชน → งานเดียวกิน 2 โฟลเดอร์ 2 วัน) · fallback ชื่อ+วัน ใช้เฉพาะโฟลเดอร์ที่**ไม่มี manifest** (ของทางการ์ด) · โฟลเดอร์ที่นิกเปลี่ยนชื่อเองหลังจัดแล้ว = ตามไปลงโฟลเดอร์นั้น ไม่สร้างใหม่
- 📦 **`import_batch(folders, dest_root)`** — งานที่ส่งมาพร้อมกันและเป็นงานเดียวกัน (job_name+work_date ตรงกัน) ถูกจับรวม**ก่อน**กฎวันที่จะทำงาน → 1 โฟลเดอร์ 1 วัน · คืนผลลัพธ์ 1 อันต่อ 1 โฟลเดอร์ที่ส่งมา (สำหรับ receive log) · โฟลเดอร์ที่ยังโอนไม่เสร็จ/พัง = รายงานแล้วข้าม ไม่ทำให้อันอื่นล่ม · **map ชื่อรูปแยกรายอาไรวัล** (2 เครื่องส่ง `0001.jpg` เหมือนกัน) · ลำดับ: work_date เก่าสุดก่อน แล้วตัดสินด้วย created_at (งานที่ทำก่อนได้สิทธิ์วันนั้น)
- 📝 **สเปก `job.json` v1 ถูกหั่น 2026-09-22** — `photos[]` = ชื่อไฟล์เปล่าๆ · **tag Before/After + notes + spare_parts ตัดออกหมด** (EMR ทำเองอยู่แล้ว · EMR ออกจากโปรเจคนี้) · HPO อ่าน**ได้ทั้ง 2 แบบ** (ชื่อเปล่า / object) แล้วเขียนกลับแบบเดิมที่รับมา
- 🚢 `get_dest_root(ship)` / `remember_dest_root(ship, path)` — จำโฟลเดอร์ปลายทางรายเรือ (เก็บใน auth config)
- ⏭️ **ยังไม่ทำ:** step 4 (LAN receiver + receive log + undo merge) · step 5 (QR pairing + ส่ง catalog ให้มือถือ) · ยังไม่ build/ไม่ปล่อย release (ยังไม่มีปุ่มใน UI)
**v1.045 ที่ต้องจำ:**
- 🏷️ **ชื่อไฟล์รูป = ชื่อโฟลเดอร์ + ลำดับ** (`DD-MM-YY <Job>_001.jpg`) — เดิมทุกโฟลเดอร์เริ่มนับ `img_001` ใหม่ นิกย้ายรูปข้ามโฟลเดอร์ทีต้องเปลี่ยนชื่อเองทุกที · rename เกิดที่ **Phase 4 ไม่ใช่ Phase 1** (ชื่อโฟลเดอร์สุดท้ายยังไม่รู้ตอน resize — AI ยังไม่ตั้งชื่อ + วันที่ยังถูกยุบเข้าเดือนเป้าหมายได้) · merge เข้าโฟลเดอร์เดิม = นับต่อ 003,004 ไม่ใช่ `_001_2` · `photo_prefix()` ตัดชื่อให้ path ไม่เกิน 259 ตัว (ชื่อโฟลเดอร์ถูกซ้ำเข้าไปในทุกชื่อไฟล์) · rename พังไม่ทำให้ commit พัง · **โฟลเดอร์เก่าไม่แตะ** (นิกเลือก "ทำแค่ของใหม่")
- ⚠️ **`Path.write_text()` บน Windows แปลง LF เป็น CRLF** — เขียนไฟล์ซอร์สด้วย python ต้องใช้ `open(..., newline="")` เสมอ (ตระกูลเดียวกับบั๊ก BOM ของ v1.044 · เทส `test_no_source_file_carries_a_bom_or_cr` จับได้)
**v1.044 ที่ต้องจำ:**
- 📅 **วันที่ถ่าย = ตัวหลัก · 1 วัน = 1 โฟลเดอร์** (Nick ตัดสิน 2026-09-02) — `grouper` ห้ามตัดกลางวันด้วย time gap อีก · gap เหลือหน้าที่เดียว = เชื่อมชุดที่ถ่ายคร่อมเที่ยงคืน · ยังยุบเข้าเดือนเป้าหมายเหมือนเดิม · ผล: 48→12 โฟลเดอร์, shifted 29→0
- 💾 **`data/job_catalog.json` = ข้อมูลผู้ใช้ที่บังเอิญอยู่ในกองไฟล์ที่ installer ทับ** — ทุกอัพเดทตั้งแต่ v1.025 ลบชื่องานที่ Nick สอนทิ้งเงียบๆ · ตอนนี้ installer snapshot ก่อนแตกไฟล์แล้ว merge คืน (`_read_installed_catalog` / `_merge_catalog`) · catalog ที่ ship = 174 ชื่อ
- ⚠️ **PowerShell `Set-Content -Encoding utf8` ใส่ BOM เสมอ (PS 5.1)** — `str.strip()` ไม่ลบ U+FEFF → `_parse_version` อ่าน `1.044` เป็น **(0,44)** → updater เด้งอัพเดทวนไม่จบ · `read_version()` ใช้ `utf-8-sig` แล้ว · **เทสที่เทียบไฟล์กับค่าที่อ่านจากไฟล์เดียวกันจับบั๊กนี้ไม่ได้**
- **เดิม v1.043 ที่ต้องจำ:**
- 🖼️ **CTkButton 5.2.2: ถ้าสร้างด้วย `image=None` แล้วค่อย `configure(image=...)` ทีหลัง = รูปไม่ขึ้นตลอดกาล** (`_image_label` สร้างใน `_draw()` เท่านั้น และ key `"image"` ไม่ trigger redraw) → **ต้องสร้างปุ่มพร้อม placeholder image เสมอ**
- 🧵 **ห้ามเรียก `self.after()` จาก worker thread** — Tkinter รับไว้เงียบๆ แล้วไม่ยิง (แถวแรกที่จบก่อน mainloop จะหายเสมอ) → ให้ worker ใส่ `queue.Queue` แล้วฝั่ง Tk โพลเอง
- 🏷️ badge หัวบนต้องอ่าน `auth.get_model()` ไม่ใช่ `tier.label` (พรีเซ็ต hard-code ชื่อรุ่นไว้ → โกหกได้)
- 🗑️ `processor.discard_assignment()` = ลบเฉพาะโฟลเดอร์ `__pending_` ใต้ dest + ไฟล์ที่ resize เท่านั้น · **ปฏิเสธ** ถ้าอยู่นอก dest หรือไม่มี `PENDING_MARKER` · **ต้นฉบับของ Nick ไม่แตะ**
- 🚫 analyzer มี `irrelevant` แล้ว (สกรีนช็อต/คน/อาหาร/วิว) → ไม่ตั้งชื่องานให้ + ไม่เข้า fuzzy match · คนใส่ PPE ทำงานกับเครื่อง = งานจริง ไม่ใช่ irrelevant
- **เดิม v1.042** (2026-08-05 layout fix) · **~7,205 LOC / 32 ไฟล์** (core 15 + ui 7 modules, main.py 1,247) · catalog **146 (121 bundled + 25 user)** · 14 formats · ผ่าน audit 8 รอบ (Cos) + **Tester round v1.041** (3-agent, 18 code fixes)
**Layout v1.042 (จอเล็ก):** แถวบน = 3 คอลัมน์ (Step 1 | Step 2 | Log, ~200px) · **Step 3 = พื้นที่หลัก** — pack `side="bottom"` ก่อน top_row (จอเตี้ย → top_row โดนบีบแทน Step 3) + table_scroll `height=150` ขั้นต่ำ · drop zone 64px · log box 110px · sources list max 3 บรรทัด · wraplength 280
**Tests (จริง):** `tests/test_core.py` = **27/27 PASS** (pure-Python, ไม่ต้องใช้ key/รูป) · `scripts/smoke_test.py` = live Gemini smoke 6/6 (มี synthetic-image fallback). ⚠️ คำว่า "67/67 tests" ใน docs เก่า = ของปลอม (ไม่เคยมี test suite) — แก้แล้ว
**Tester v1.041 fixes สำคัญ:** grouper ข้ามเที่ยงคืน · date allocation เดือนเต็มเก็บวัน EXIF เดิม · analyzer JSON raw_decode · tier→model wiring · auth null-safe · settings scale revert on Cancel · installer atomic auth.json · Thai→EN strings

**Build/Release:** `dist/HappyPhotoOrganizerSetup.exe` (84.7 MB) · **v1.042 ปล่อยขึ้น GitHub Releases 2026-08-05** → `/releases/latest` = v1.042 (auto-updater เห็น, ไม่ใช่ prerelease) · tag `v1.042` · auto-updater ผ่าน GitHub Releases API

**Known limitations:** Phase 1 ช้ากว่า Nick_Resizer 5-10x (iterative quality) · AI accuracy ขึ้นกับ catalog completeness · ไม่มี undo Phase 4 · ทดสอบ AI จริงต้องมี key + รูปจริง

**V2 roadmap:** เติม .docx form อัตโนมัติจากชื่อโฟลเดอร์ + AI-generated รายละเอียดงาน (python-docx/docxtpl) — รอ Nick ยืนยันก่อนเริ่ม

**Branding (อย่าสับสน):** 📷 camera (`happy_icon.ico`, `happy_logo*.png`) = identity · 🤖 robot (`mascot.png`) = helper

**อย่าทำ (decided):** ห้าม revert UI Thai · ห้ามเอา mascot ไปช่อง identity · ห้ามเปลี่ยน default model · ห้ามใส่ bounce animation mascot · ห้ามลบ uninstaller/registry · ห้าม refactor Phase 2 เป็น sequential
