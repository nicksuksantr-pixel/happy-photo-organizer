# CHAIN_ALIGNMENT — JobShot → Happy Photo Organizer → Engine Maintenance Report

**Issued 2026-09-26 by the Director session** (`cwd` = `…\Documents\Projects\Test-Memory-Structure-Workflow`)
**on Nick's rulings of that evening**, after reading all three projects' full reports
(`docs/DIRECTOR_REPORT_2026-09-26.md` in each repo) and measuring the artifacts on disk.

**This file is authoritative for the three projects on every point it states.** Where it
contradicts a project's own document, this file wins until a project shows it is wrong — and
showing that is welcome (§12). Synced into every repo's `memory/` by `sync-rules-to-projects.ps1`;
the master is `Claude\Projects\Nick\CHAIN_ALIGNMENT.md`.

> **Nick's instruction with this document:** *"If anyone has a better proposal, send it back to us
> and we will discuss it. If anyone is out of alignment, fix yourself. I am waiting for the summary
> so I can start testing for real."*

---

## §1 · How to respond — and it is not by message

**7 cross-session messages were sent to the Director on 2026-09-26 and exactly 1 arrived.** Two of
HPO's returned `success` and delivered nothing.

- **Agreement:** nothing to send. One line in chat to Nick.
- **A correction, a better proposal, or a clause you cannot implement:** write
  **`docs/CHAIN_ALIGNMENT_RESPONSE.md`** in your own repo, cite the clause number, push it, and say
  one line in chat. The Director reads git.
- **Do not edit this file.** Your `memory/CHAIN_ALIGNMENT.md` is a mirror; the sync overwrites it, and
  since 2026-09-26 `sync-rules-to-projects.ps1` **aborts the entire fleet sync** if a mirror holds
  content the master lacks. Same rule as `SHARED_LESSONS.md` (#10.1).

---

## §2 · The three roles — no overlap

| | Role | The one thing it alone decides |
|---|---|---|
| **JobShot (JS)** | Capture the work where it happens | What was done, and whether a value was typed or confirmed |
| **Happy Photo Organizer (HPO)** | File it into the archive | **The folder's date, its name, and the archive photo names** |
| **Engine Maintenance Report (EMR)** | Produce the controlled form `F-04-TEC/03` | What reaches the company's document |

**The engineer types the report once, on the phone.** That is the point of the chain. No step after
it retypes anything, except for a job captured before JS v0.026 (§9).

---

## §3 · The journey of one job

**On the phone (JS)**

1. Create the job: ship, date, name (action chips + equipment; optional ✨ Thai → the archive's English).
2. Photograph, and **tag each photo Before / After / part-label.** The phone cannot infer this.
3. Optionally fill the report page: equipment / model / serial, preventive|breakdown, pass|fail, the
   four sections, spare parts.
4. **Provenance is set here.** An AI-suggested value enters a box only when the engineer taps it, and
   the tap marks it `ai_confirmed`; a typed value is `typed`. **The phone never writes a bare `ai`.**
5. `finalise()` writes in this order and no other: renumber photos → `emr.json` → **`job.json` LAST.**
   `job.json` is the completion marker; HPO keys on it.
6. One tap sends the job to the paired PC over the LAN. (Zip via share / save dialog is the fallback.)
7. The receipt naming the filed folder is **the only thing that makes deleting the job from the phone safe.**

**On the PC (HPO) — no human step in this segment**

8. The receiver runs while the app is open; the phone is paired once per PC by QR (host · port · vessel · token).
9. Gate the zip: images and JSON only; `"jobshot"` must be the integer `1`; `job_name` non-empty;
   `photos[]` non-empty.
10. A job from the phone **skips the AI naming and the review entirely** — it arrives already named and grouped.
11. Resize into the 10–25 KB band, name the folder `DD-MM-YY <Job name>`, rename every photo after its folder.
12. **Choose the day number — see §5.**
13. Write HPO's own `filed` block into the archived `job.json`: `folder`, `folder_date`, `work_date`,
    `date_shifted`, `merged_into_existing_folder`, `grouped_with`, `extras`, **`renamed`**, `hpo_version`,
    `filed_at`.
14. Copy `emr.json` **byte-for-byte and never parse it.** Carry any other `*.json` sidecar the same way
    (a name matching `job.json` / `job-*.json` is read as a manifest, not carried).
15. Answer the receipt, and record it in `jobshot_filed.json` so `GET /jobshot/v1/job/<id>` can answer a
    lost reply.

**On the PC (EMR)**

16. Open the month folder, click the job; the form fills.
17. Read `emr.json` — gate `"emr": 1`; **every other field optional**; an unrecognised shape is dropped
    **per field**, never wholesale, and **the reader never raises.**
18. Read `job.json` for exactly three keys: `filed.extras`, **`filed.renamed`**, `filed.folder`.
    **No content ever comes from `job.json`.**
19. **Drop any value not marked `typed` or `ai_confirmed`.** Refuse with a stated reason in three cases:
    two manifests naming one draft, `filed` without `renamed`, photo names resolving to nothing.
20. The engineer presses **Create report.** The `.docx` is written beside the photos, and the folder then
    remembers the job is done (a `.docx` present = done).

---

## §4 · Ownership — the boundaries that stop the three disagreeing

| Decision | Owner | What the other two do |
|---|---|---|
| **The folder's date** | **HPO alone** | JS sends `work_date` as a *fact about the work*, never as the filing decision. EMR prints **the folder's** date and applies `work_date` / `work_date_end` to nothing. |
| Folder name, archive photo names | HPO | — |
| **`filed.renamed`** — the phone-name → archive-name join | **HPO alone** | JS never reads it and must never start. **EMR depends on it completely and cannot verify it** (§6). |
| Provenance (`typed` / `ai_confirmed`) | **JS alone** | EMR enforces it. |
| Report content | **The engineer, once, on the phone** | HPO does not touch it. |
| The company form | **EMR alone** | `work_date_end` is **removed** (JS v0.028): the form has one date field and no period-of-work line. A span that matters prints as prose inside Troubleshooting, which already flows from `sections.action`. **Closed by Nick; EMR's `CHAIN_CHECKLIST.md` §4d closes with it.** |
| `_RANGE_RE` in EMR | EMR | **Kept deliberately.** The chain will never feed it again, but it reads multi-day folders that already exist in Nick's archive and folders another crew names by hand. *No longer fed is not dead.* |

---

## §5 · THE DATE RULE — Nick's ruling, binding on all three

> **HPO looks in its own destination folder, on the real machine's path, for a day number in that
> month that is not yet used. Every free day is filled before any day number repeats. Once the month
> has no free day left, repeating a day number is allowed.**

1. **`work_date` from the phone is an input, not the answer.** HPO may move the folder to a free day;
   when it does, `filed.date_shifted` is `true` and `filed.work_date` still records what the phone said.
2. **Unique while any day is free. Duplicates only when the month is full.**
3. **EMR prints the folder's date.** Duplicate day numbers are safe there — jobs are keyed by *path*,
   `is_done` asks only whether a `.docx` sits in that folder, and the list sorts by `(date, name)`.
4. **A folder name must never carry a date range.** `assign_unique_dates` can hand a day inside the
   range to another folder in the same pass, so reader and writer would disagree within one run.
5. **The mechanism, stated so nobody re-derives it wrongly:** HPO's used-day scan reads **the folders
   present on disk**, not `jobshot_filed.json`. **Deleting a folder returns its day to the pool**, and a
   later job may legitimately take that day again. The duplicate day numbers observed on 2026-09-26
   (day 26 ×2, day 01 ×2 in one destination, while 4 of the 5 filed folders had been removed) are almost
   certainly this and not a defect — **HPO: confirm against your own `scan_used_days()` and say so.**
6. ⛔ **How to test this rule — Nick cannot create 30 jobs.** Do **not** test by deleting folders and
   watching what happens; that exercises the pool-release path, not the full-month path. **Create empty
   placeholder folders to fill every day of a test month, then file one more job and record what it does.**
   All three projects must be able to state the same expected outcome before the test runs.

---

## §6 · The three fragile joins — a guard the other side props up

Read `SHARED_LESSONS.md` → **`a-guard-the-other-side-props-up-is-not-a-guard`** first. Each of these
passes every test today, for as long as the other side keeps a habit.

| # | The join | Who fixes it |
|---|---|---|
| 1 | **HPO's vessel guard is `if got and got != wanted`** — a manifest with **no `ship`** skips the check entirely and is filed into whatever tree this PC points at. It holds only because JS always writes `ship`. | **HPO proposes, Nick approves.** The fix refuses input accepted today, so it does not ship unilaterally. HPO's proposal — *"when this PC has a ship configured and a manifest carries none, refuse it"* — is accepted in principle by the Director; **it needs Nick's word and JS's agreement before it ships.** |
| 2 | **EMR's `source.get(name, "typed")`** — absent provenance means typed. Flipping that default silences every hand-typed spare part in the fleet while the report still looks complete. Untested until 2026-09-26 because JS always writes the map. | **EMR** — now tested from a JS-generated fixture of a shape JS cannot emit. **JS must announce before it can ever write a bare `ai`**, which is also the release trigger for EMR v0.3.6. |
| 3 | **EMR trusts `filed.renamed` absolutely** — if HPO wrote a stale or partial map, EMR would tag the **wrong** photos rather than refuse. No cross-check exists on EMR's side. | **Nobody, deliberately** — a capture time or hash in the map is a real contract change and the failure has never been observed. **Recorded as known, not assumed.** HPO has frozen the `filed` block's key set and types in a test; if it goes red the question is *"has EMR been told"*, never *"fix the test"*. |

**Standing rule for all three:** when a guarantee of yours rests on another project's habit rather than
on your own code, **write down which side is holding it up.** *"Covered by a test"* and *"covered by their
habit"* look identical from inside and behave differently exactly once.

### §6.4 · Two silent-loss paths found on 2026-09-26 in the real run — added after issue

**§6.4a — `spares` and `spare_parts` are both accepted, and sending both loses everything.** EMR
resolves them as `raw.get("spares", raw.get("spare_parts"))`, so the fallback runs **only when
`spares` is absent**. A writer that emits `"spares": null` **alongside** a populated
`"spare_parts"` loses every row **silently, with `dropped: 0`** — nothing was refused, because
nothing was offered. **JS sends exactly one of the two keys.** EMR has specified it and pinned it
with a test rather than guessing which key a writer meant. Full field-by-field spec:
`Engine_Maintenance_Report/docs/SPARE_PARTS_SPEC.md`.

**§6.4b — the ship name on the controlled document can differ from the job's, silently.** By EMR's
contract §0 the Ship Name comes from **EMR Settings** and never from the phone. Measured on the
21:27 job: `emr.json` says `ship: ENA Test`, and the issued `.docx` prints **`ENA Challenger`**.
Working exactly as specified, and harmless tonight because both are test values — **but this is a
controlled document that names a vessel.** Not fixed unilaterally by anyone: **if the phone's
`ship` is ever to be authoritative, that is a contract decision and it is Nick's.** Until then all
three know the two can disagree, and EMR is not at fault for the divergence.

---

## §7 · What each project guarantees

**JS guarantees** (committed in writing, v0.028): `job.json` is the eight fields with `"jobshot": 1`,
written last · photos byte-for-byte with EXIF intact · `emr.json` absent when nothing was typed · photo
names in `emr.json` true at write time · **`source` written for every spare row and every section** ·
**`equipment` stays a bare string** · **only `typed` or `ai_confirmed` is ever written.** Any change to
the last three goes on the chain sheet **with a version, before a line of code is written.**

**HPO guarantees:** `emr.json` carried byte-for-byte · `extras` naming what is on disk · `renamed`
mapping every filed photo (v1.055+; earlier folders have no map and cannot get one) · a key absent from
`renamed` means that photo was never filed · `filed.folder` / `extras` / `renamed` keep their names and
types, **frozen by a test.** Named gap: `extras` reports that `copy2` returned without error, **not that
the bytes on disk are complete.**

**EMR guarantees:** it never writes to another project's files · a bad draft never fails a report · it
refuses rather than half-fills an ambiguous folder · unconfirmed values are dropped, not printed ·
`parts` photos are never tagged and never printed.

---

## §8 · How a version reaches each app — measured, not assumed

| | Route | What that costs |
|---|---|---|
| **JS (phone)** | **There is no in-app updater.** `AppVersion.isNewer()` exists and **only tests call it.** Every version reaches the phone by someone building the APK and installing it over USB. | ⚠️ The phone cannot be updated, and its version cannot be verified, unless it is plugged in. The APK is **debug-key signed**; moving to a real keystore **requires an uninstall, which deletes the jobs on the phone.** A split-per-abi APK and a fat APK **cannot be installed over each other** (`versionCode` 2000+N vs N). |
| **HPO** | In-app updater, GitHub release, SHA-256 from the release notes. | The update cache reclaims itself after an install (measured 2026-09-26: 87.4 MB → 0). |
| **EMR** | In-app updater, `nicksuksantr-pixel/EMR-releases`, SHA-256 from the release notes, swap-by-rename on exit. | **It re-downloads the full 41.7 MB even when a verified zip is already staged** — `download()` has no already-have check, and a tie goes to the internet because the folder road carries no checksum. Deliberate, and written here so it is not discovered on a ship's link. |

---

## §9 · Proven and not proven, as of the night of 2026-09-26

**Proven by artifact — `Downloads\26-09-26 Inspected Tumble Dryer\`, 2026-09-26 21:27.** Nick ran a
real job end to end and pressed Create report:

```
created_at (JobShot) 2026-09-26T21:24:19+07:00
filed_at   (HPO)     2026-09-26T21:27:51        <- 3m32s AFTER creation
hpo_version          1.057      renamed 7/7     date_shifted false
document             …Inspected Tumble Dryer.docx, 517 KB, written 21:32
in the document      7 media = 3 template + the 4 TAGGED photos; the 3 `parts` label
                     shots correctly absent, exactly as EMR guarantees
provenance           problem ai_confirmed · cause typed · action typed · test ai_confirmed
                     -- all four sections passed the gate, none dropped
```

**The full chain has completed: phone → HPO v1.057 → EMR v0.3.5 → a printed `F-04-TEC/03`.** Nick
ran it, not a harness, so it is testimony. §10's watched run is still worth doing as a second pass.

### ⛔ The first version of this clause was wrong, and how it was caught is the clause that matters

**This section originally cited `26-09-26 Overhauled Air Compressor No. 1`. That folder is
JobShot's generated fixture, not a chain artifact, and its own timestamps say so.** EMR refused the
credit and produced the measurement (`docs/CHAIN_ALIGNMENT_RESPONSE.md`):

```
created_at 2026-09-26T16:26:55+07:00
filed_at   2026-09-26T15:40:00        <- FORTY-SIX MINUTES BEFORE it was created
```

**HPO cannot file a job that JobShot has not produced yet.** Two more marks of a constant rather
than a clock: `created_at` carries an offset and `filed_at` does not, and `filed_at` is a round
minute to the second. Corroborated: the six photos are byte-identical to
`JobShot\ผู้ใช้\emr_sample_v2\archive\…`, and the two manifests differ in exactly three fields.

⭐ **The rule this earns, binding on all three: a `filed` block is data, not provenance.** The
Director read `hpo_version: 1.055` as proof HPO had filed it — a fixture generator writes that
field just as easily. **Before treating any folder as evidence of a chain run, check that
`filed_at` follows `created_at`, and that both carry a timezone.** A generated fixture is supposed
to look real; that is its job. It is the chronology that cannot be faked by accident.

⚠️ **All four of us were wrong about this one fact, in four different directions.** JS claimed the
chain for `FloodLight` (no `.docx` exists); EMR said the chain had never completed at all; HPO
correctly declined to certify a half it had not watched; and the Director asserted it from a
fixture. **The truth arrived only when Nick ran the real thing at 21:27.** No chain-wide claim goes
into any document again without naming the artifact **and its chronology**.

**Not proven, and not to be claimed:** no report from this chain has been **filed with the technical
department** · nothing has run **on another ship's PC**, a real USB stick, or a ship's link · **a full
month** has never been filled · **a merged folder** (two jobs, one folder) has never happened with real
data · **a second vessel** has never been configured · JS's zip sweep **has never reclaimed a byte** ·
the 08:00 reminder **has never been seen to fire** · EMR's hourly re-check has **never completed a real
hour** · no automated test opens HPO's window.

**Jobs captured before JS v0.026** carry no `equipment`, `maint_type` or `test_result`: three retypes in
EMR. Nothing to fix; it is history.

**Nick's ruling on the 2026-09-26 artifacts:** the destination in use that day was a **test machine and a
test path**, and the documents produced that day are **not for real use.** They are evidence about the
chain, not work product.

---

## §10 · What must be true before Nick's end-to-end test

1. **All three state the same expected outcome for §5**, in writing, including the full-month case.
2. **HPO confirms §5.5** against its own `scan_used_days()`.
3. **A real destination path** is configured, not the test path.
4. Nothing in §6 changes shape without the owner named there agreeing first.

Then Nick runs one job from the phone to the printed document **with someone watching every step**, and
whoever watches **records what they saw, not what they expected.**

---

## §11 · Secrets — Nick's ruling, all projects

A Gemini key was found in EMR's git history at `1df66b2` (2026-08-16). Nick's ruling: the key is a
free-tier key and does not need revoking, **but nothing may leak again without his permission.**

1. **No credential, key, token or secret is committed, printed in chat, pasted into a document, or sent
   to any service without Nick's explicit permission for that specific act.** A key belongs in the
   project's own `.env` or its own config directory, gitignored. (#2 already forbids borrowing another
   project's key; this extends it to disclosure of any kind.)
2. **A key that has been exposed is reported to Nick immediately, with where and when.** The decision to
   revoke or accept is his, never the session's.
3. ⛔ **A rule alone is not a fix.** 2026-09-26 proved twice over that a written lesson fires only when
   somebody goes and looks, and typing is not such a moment
   (`an-executable-check-is-the-only-lesson-that-fires-unremembered`). **An executable secret scan is
   being added by the Director session as a gate, not as a sentence.** Until it exists this clause is the
   weaker half of the answer, and says so.

---

## §12 · Disagreeing with this document

A better proposal is wanted, not tolerated. Nick's words: *if anyone has a good proposal, send it back so
we can discuss it; if anyone is out of alignment, fix yourself.*

- **Out of alignment with a clause here → fix your own project** to match, and say so in your response.
  **One exception:** a change that would **refuse input accepted today** (§6.1 is exactly this) does not
  ship without Nick's word.
- **You think a clause is wrong → say so with the measurement**, not with the argument. Every clause above
  came from an artifact or a report; replace it the same way.
- **A clause you cannot implement → say which, and why.** An unsent disagreement is the expensive kind.

*Issued by the Director session. Responses go to `docs/CHAIN_ALIGNMENT_RESPONSE.md` in your own repo.*
