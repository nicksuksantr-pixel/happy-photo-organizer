# V-Log — Happy Photo Organizer (version timeline)

Full version history, oldest → newest. Detail per version lives in `CHANGELOG.md`,
`log/log_vX.md`, and `bug/bug_vX.md`. (Created 2026-06-04 per command_pattern #11.)

| Version | Date | Highlight |
|---------|------|-----------|
| v1.001–1.023 | 2026-05-17 → 05-21 | Initial build → iterate: drag-drop, resize 10-25KB, group-by-session, Gemini Vision tagging, catalog, rename pipeline |
| v1.024 | 2026-05-22 | Feature-freeze baseline (138 jobs, 14 formats, English UI, free-tier rate limiter) |
| v1.025 | 2026-05-22 | Auto-updater via GitHub Releases |
| v1.026 | 2026-05-22 | Smart date — consolidate to dominant month, unique days across months |
| v1.027 | 2026-05-22 | Window-state persist, dropzone border, layout refactor |
| v1.028 | 2026-05-22 | pystray + zero-click silent auto-update + single-instance |
| v1.029 | 2026-05-22 | Stale-mutex fallback, hide-to-tray on minimize, Range resume download; VERSION-file refactor |
| v1.030 | 2026-05-22 | Split main.py → core/ + ui/ |
| v1.031 | 2026-05-23 | Dark dialog backgrounds, unused-import cleanup |
| v1.032 | 2026-05-23 | UpdateWorker extracted to core/ |
| v1.033 | 2026-05-23 | Double-download race fix, startup installer-cache cleanup |
| v1.034 | 2026-05-23 | ETag + If-Range download integrity, debug-log rotation, requirements pins |
| v1.035 | 2026-05-23 | Round-5: async+throttled Thai→EN translate, catalog flush on destroy |
| v1.036 | 2026-05-24 | Round-6 Cos external review — 20 patches (5xx retry, traceback capture, disk pre-check, async thumbnails, keyboard shortcuts, minsize) |
| v1.037 | 2026-05-24 | Dark Windows title bar + per-Toplevel app icon (hotfix on v1.036) |
| v1.038 | 2026-05-25 | Settings dialog Save-button clipping hotfix (CRITICAL — key couldn't be saved) |
| v1.039 | 2026-05-25 | Round-7: 11-patch sprint (transient-error regex, fallback JPEG verify, async saves, thread-safe flags, mtime-guarded sweep) |
| v1.040 | 2026-05-25 | Round-8: Settings dialog UX polish (4 paper-cuts + Risk-B deferred-callback cancel) |
| **v1.041** | **2026-06-04** | **Tester round** — 3-agent full-system audit → 18 code fixes + all doc/code mismatches corrected; first real test suite (`tests/test_core.py`, 27 tests); midnight-grouping + full-month date-allocation bugs fixed; tier→model wiring; live AI smoke test green |
| **v1.042** | **2026-08-05** | **Small-screen layout fix** — Step 3 (Review & Edit Names) is now the primary area: top row re-arranged to 3 columns (Step 1 \| Step 2 \| Log, ~200 px), Step 3 packed with shrink priority + 150 px guaranteed review-table minimum, drop zone/log/labels compacted; on short displays Step 3 no longer collapses |
| **v1.043** | **2026-09-02** | **Thumbnails finally render** (2 silent bugs: CTk 5.2.2 `configure(image=)` no-op on an `image=None` button · thread-unsafe `after()` dropping row 1) · **Gemini 3.5 Flash Lite = default** + header badge now shows the model actually in use · **AI flags non-work photos** (screenshots/people/food) instead of inventing job names · **per-folder Delete** + bulk "Delete not-work", removing the resized copies and the `__pending_` folder only · tests 27 → 35 |
| **v1.044** | **2026-09-02** | **1 shooting day = 1 folder** — the 90-min gap no longer splits a day (it only bridges midnight), so same-day work stops being scattered onto dates it was never shot on (48→12 folders, 29→0 shifted on a like-for-like corpus) · **installer merges the job catalog instead of overwriting it** — every update since v1.025 silently deleted the names Nick had taught the app (14 would have died on v1.043); shipped catalog 146 → 174 · **BOM in VERSION** made `_parse_version` read (0,44) and would have looped the auto-updater · tests 35 → 38 |
| **v1.045** | **2026-09-02** | **Photos are named after their folder** (`DD-MM-YY <Job>_001.jpg`) instead of restarting at `img_001` in every folder — moving a photo between folders no longer collides and no longer needs a manual rename; merging into an existing day continues the numbering (003, 004…) instead of falling back to `_001_2`; the name is trimmed to keep the full path inside Windows' 260-character limit · tests 38 → 43 |
| **v1.046** | **2026-09-22** | **HPO becomes the receiver for JobShot (phone)** — steps 1–3 of the R&D Director brief: `core/jobshot.py` files an arrived job headlessly (no UI, no AI, no re-grouping — the engineer already grouped and named it), the commit's photo rename now **returns its old→new map** so `job.json`'s `photos[].file` follows it (Before/After tags would otherwise be orphaned) and **skips non-images** so the manifest is not renamed into a `.json` photo; `work_date` vs the archive day-rule settled by Nick — **the rule wins**, the shift is recorded in the manifest's new `filed` block, and a same-job-same-day folder is merged rather than pushed to a free day; dest_root remembered per vessel · **one real job = one folder**: an arrival is matched to its own filed folder on job_name + work_date (the day rule alone gave one job two folders on two dates), and `import_batch()` groups arrivals that land together before the day rule runs · manifest `photos[]` reads both the new plain-name shape and the older objects (Before/After tags were cut - EMR owns that) · tests 44 → 61 · not built/released (no UI entry yet) |
| **v1.047** | **2026-09-22** | **The JobShot import gets a way in** — v1.046 finished it and left it unreachable (it appeared in no UI file). Now: drop a job folder (or the whole JobShot directory) on the drop zone, a **From phone** button, and `scripts/import_job.py` for when the app will not start · destination remembered per vessel, asked once · a folder with no `job.json` is reported as *not ready yet* and never touched · **manifest version gate accepted `true` and `1.0`** (`True == 1` in Python) — now demands a real int, tested as a sweep of wrong shapes · tests 61 → 64 + an end-to-end run through the real script |
| **v1.048 → v1.049** | **2026-09-23** | **Jobs arrive over Wi-Fi** (built for testing; **shipped inside v1.050**) · v1.049 aligns `hello` with `LAN_PROTOCOL` §2 (`app` was a display name where the document specifies an identifier the phone may compare; `ready:false` now carries its reason) and **pins the wire shapes in tests** after a reply-shape deviation reported a perfect upload to the phone as a refusal — pair by scanning a QR in HPO, the phone sends, HPO files it exactly as a dropped folder · receiver runs while HPO is open and only once paired · `GET /jobshot/v1/job/<id>` answers *did you already file this* so a lost reply costs a question instead of a 20 MB resend, and receipts are written by every route · hostile zips refused (zip-slip, bombs, non-job files) into quarantine before anything reaches the archive · a refusal that Windows turned into a connection reset, so the phone read *wrong token* as *Wi-Fi dropped* · tests 64 → 88 |
| **v1.050** | **2026-09-24** | **The updater becomes visible** — an update button in the header that says what it is doing (`Checking…` · `Up to date (1.050)` · `Updates: offline` · `Downloading 37%` · `Install v1.051`) and does the obvious thing for that state · a check that finds nothing says so, an install asks first, and neither happens mid-batch · before this the only manual check was a right-click on the tray icon and the only sign of activity was a log line before the app restarted itself · also fixed a test that could fail one run in sixteen (its "wrong token" was the real one whenever that ended in 0) · tests 92 → 96 |
| **v1.051** | **2026-09-24** | **The buttons were off the edge of the header** — four 30 px buttons stacked into a 92 px header needs 132, so Phone was cut in half and the update button added the day before was entirely below the fold: the control built to make updates visible was itself invisible. Now a grid, with the update button spanning the width its own labels need · **"cannot connect" was the opposite** — the log shows three `GET /jobshot/v1/hello` answered 200; the phone was paired and being told *no destination folder chosen on the PC yet*, with the fix sitting behind the window being looked at. The pairing dialog now separates *paired* from *ready* and carries the folder picker itself |
| **v1.052** | **2026-09-24** | **The moved date explains itself** — three jobs sent on one day were filed as `24-09-26`, `04-09-26` and `18-09-26`, which is the unique-day rule working (Nick confirmed it a third time) but read as a fault. The reply to the phone now carries `work_date` next to `date_shifted` so JobShot can say *moved from 24-09-26*, and the PC log names both dates and the reason · **a receipt write that silently did nothing now reports** · the flaky tampered-token assertion found in a second test, and its pattern grepped out |
| **v1.053** | **2026-09-25** | **The report draft rides along** — a job may now carry `emr.json` (the report draft typed at the machine, which EMR reads back out of the folder HPO builds). Before this the wire dropped it during extraction, the importer would not have copied it anyway (its file list comes from `photos[]`), and the receipt said nothing — while `filed` is what the phone turns into *safe to delete*, so Nick could delete the only copy of something he wrote. Now: images + JSON and nothing else (`notes.txt`, `run.exe`, `photo.jpg.exe` still refused, with a test — widening a gate for a friend is when a gate gets widened for everyone) · copied never moved, bytes and name intact, the second of a merged pair kept as `emr-<job_id>.json` · `"extras": ["emr.json"]` in the reply and in the manifest, and a copy that fails is left out of it · **Nick's day rule** (reuse a day only once the month is full) was already true since v1.026 — verified by running it, and now pinned by a test · tests 96 → 101 |
| **v1.054** | **2026-09-26** | **The lost-reply route can speak about the draft** — filling in HPO's part of Nick's one-sheet chain checklist (`docs/CHAIN_CHECKLIST.md`) found that `GET /jobshot/v1/job/<id>`, the route that exists for when the upload reply is lost, could say the photos were filed and stay silent about the report draft — the one file Nick types by hand. Raised on the sheet instead of shipped (three sides were checking against a released wire); JobShot voted yes **and found the matching bug on their side**: their `markFiled` replaced stored extras with the latest receipt's, so a resend hitting §4 overwrote a confirmation the PC had already given · now `extras` is in §4 too, read from the archived manifest when the receipt book has no answer, so it works for jobs filed before this version — and an index entry with **no `extras` key** is treated as *unknown*, not as *none* · a merged folder answers per job (`emr-<job_id>.json`) · `"not a job"`, which JobShot prints verbatim to Nick, became **"no job.json in it — send the job again"** · the §4 key set turned out never to have been frozen like §2 and §3 — pinned now · tests 101 → 109 |
| **v1.055** | **2026-09-26** | **The draft's photo names now lead somewhere** — EMR built a folder the way HPO files it, ran its scanner and measured **zero matches**: `emr.json` names the phone's photos (`0001.jpg`), the folder holds the archive's (`26-09-26 Pump Overhaul_001.jpg`), and EMR skips a name it cannot find **in silence** — a report would have printed with no photos while saying the draft was applied. Three correct programs, one join that did not exist; only findable by the side that reads the folder building it. Each job's manifest now carries `filed.renamed` (phone name → archive name) next to `filed.extras` · **per job, never per folder** — every phone numbers from 0001, so a merged folder has two `0001.jpg` and one flat map would point one job's report at the other job's pictures (JobShot caught that before it was written) · the map already existed at filing time and was being thrown away · **and the obvious pairing rule is wrong**: when the first job files no draft, the second's draft keeps the plain name `emr.json` while its manifest is `job-<id>.json`, so `filed.extras` is the only correct link · not put on the wire (the phone has no use for archive names) · tests 109 → 112 |
| **v1.056** | **2026-09-26** | **You can clear a finished batch** — Nick: *"เคลียงานจากลิสเพื่อทำใหม่ไม่ได้ ต้องปิดโปรแกรมเปิดใหม่"*. There was no control at all: the only code that emptied Step 3 lived inside *Start AI Tagging*, so clearing was a side effect of starting the next run, and the button labelled *Clear* only emptied Step 1. Writing it down found two more — **Commit stayed armed over an already-filed batch** (pressing it errored every row of a run that had succeeded) and **the commit left its sources loaded while the drop zone appends**, so the next drop re-ingested the batch just filed; Nick was saved only by the accident that his restart cleared both · all three fixed together at his word, plus an older one the new button would have promoted: Phase 1 walked back into an abandoned `__pending_` folder and would have filed its leftovers under the next job's name · **three rounds of independent review, every round said do not ship**, and the sharpest findings were in my tests: three of them blocked on a real modal dialog when run alone while passing in company, and the new abandoned-folder test was vacuous (its orphan could never have collided) · tests 119 → 130 |
| **v1.057** | **2026-09-26** | **It remembers your folder** — Nick: *"เปิดปิดหรืออัพเดทโปรแกรม ช่องโฟลเดอร์ที่เลือกไว้ไม่จำ"*. Three pickers, only two of them ever persisted it, and **nothing read it back at startup** · the costly half was the second one: the phone receiver reads that same variable, so every restart told the phone *"no destination folder chosen on the PC yet"* about a folder that had never moved · now one `_set_dest()` that all three use, written under the vessel **and** PC-wide (Nick renamed his vessel today — the per-vessel key alone would have lost it), restored at startup before anything asks whether this PC can receive, and never restored when the folder is gone · a save that fails says so (both calls RETURN failure rather than raising, so the wrapper I wrote was dead code) · **and the restore broke the per-vessel destination in the same hour**: `_jobshot_dest`'s early return skipped reading the job's own manifest, so a USB-stick job from another ship would have been filed here silently — the hand-drop path has no vessel guard, unlike Wi-Fi · tests 132 → 134 |
